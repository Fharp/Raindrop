# 降水渲染谱 · 接口约定

后台把气象数据编译成「渲染谱」，前端只做一件事：照谱开关增益、按点放雷。
所有判断在后台完成，前端不含任何气象逻辑。

```
pythondownload.py  ──►  openmeteo_out/{slug}_index.csv
                                      {slug}_meta.json
                              │
                    rain_engine.py      （离线，改参数就重跑）
                              │
                        web_out/  ──►  rain_select.py  ──►  HTTP
                                            （运行时选片）        │
                                                                前端播放器
```

---

## 1. 目录

| 路径 | 内容 |
|---|---|
| `web_out/manifest.json` | 素材表、播放参数、阈值、标定方式、覆盖区间 |
| `web_out/index/cities.json` | 城市名册，含每城 24 个 UTC 钟点的候选数 |
| `web_out/index/city/{slug}.json` | 单城事件摘要、钟点索引、本地日期表与 UTC 日期表 |
| `web_out/index/hour/{HH}.json` | UTC 钟点 → `{城市: [[事件号, 帧号], …]}` |
| `web_out/scores/{slug}/{event}.json` | 逐小时渲染谱，前端真正要拉的那一份 |

体积参考：七座城 1007 场雨 3391 可播小时 ≈ 3.3 MB。按你实际的 57 座城
约 25000 可听小时外推，全量约 25 MB；单场谱多在 2–8 KB，前端一次只拉一份。

---

## 2. 播放契约

**帧**。一帧 = 一个数据小时 = `frame_seconds` 3600 秒实时播放。
`frames[i].stems` 是该帧每条素材的**最终绝对增益**（线性，0–1），
前端把每条 stem 接一个 `GainNode` 直接写进去即可，不需要再乘任何系数。

**渐变**。跨帧时所有 stem 在 `ramp_seconds`（默认 30 秒）内线性过渡。
用 `setTargetAtTime(target, t, ramp/3)`，别直接赋 `.value`，否则有拉链噪声。

**缺席即静音**。`stems` 里没有的键表示该层这一小时不发声（增益已低于 0.005 被裁掉）。
前端应保留全部六条循环层常驻，把增益推到 0，而不是 stop 掉再重建。

**雷**。`frames[i].thunder` 是一次性事件数组：

```json
{"at": 1832.4, "asset": "thunder_near_07", "gain": 0.71, "pan": -0.32, "rate": 1.04}
```

`at` 是该帧内的秒偏移。`rate` 是播放速率，±8%，用来掩盖素材重复，
接到 `AudioBufferSourceNode.playbackRate` 上。中途切入某一帧时，
`at` 早于切入点的雷直接丢弃，不要补放。

**风的阵性**。`frames[i].wind_lfo = {rate_hz, depth}` 存在时，
挂一个 `OscillatorNode → GainNode(depth × 该层基准增益) → 风层 gain 的 AudioParam`。
AudioParam 上的多路连接是相加的，这样基准值由 `setTargetAtTime` 控，
起伏由 LFO 叠加。`frames[i].wind_pan` 由风向换算，可选。

**`ui` 字段**只喂界面，与发声无关。降雪只出现在这里。

---

## 3. 三种模式

`rain_select.py` 已实现，包一层 HTTP 即可；也可以在构建期烤成静态 JSON。

| | 随机（默认） | 城市随机 | 指定日期 |
|---|---|---|---|
| 取值范围 | 全部城市 | 该城全部事件 | 该城该日事件 |
| UTC 钟点对齐 | 是 | 是，落空时降级 | 是，对不上则从头 |
| 结束行为 | 换城 | 换城 | 循环，无限听 |
| 降权池 | 生效 | 生效 | 不生效 |

**切入点**。选中 `(事件, 帧号)` 后，起播位置 = 帧起点 + `second_offset`，
`second_offset = 当前 UTC 分 × 60 + 秒`。这样播放器的钟与 UTC 钟同步，
往后一直保持对齐——播满一小时，正好跨到该场雨的下一小时。

**降权池**在**城市**层面等权，不按事件数等权。否则新加坡 343 场、台北 189 场
会把银川 21 场彻底淹掉。听过一次权重乘 `0.5`，下限 `0.05`，
历史由前端 `localStorage` 存、请求时回传。

**城市随机落空**。该城在当前 UTC 钟点从未下过雨时，返回
`hour_aligned: false` 与 `notice.coverage_days`，前端出
「过去 N 天里，这座城市在此刻还没有下过雨」，同时照常给一场非对齐的雨。

**日期表两套**。`dates_local` 按城市当地日历，`dates_utc` 按 UTC 日历，
同一场雨在两套里可能落在不同日子。日期选择器默认用 `dates_local`，
你要的「切换到用户所在时区」开关，在前端拿 `dates_utc` 再换算即可。

---

## 4. 降水结束之后

`next_after(slug, event_id)` 返回其一：

- `{has_next: true, wait_hours: N, next_event_id: …}` → 「下一场雨在 N 小时之后」
- `{has_next: false, hours_to_coverage_end: N}` → 「未来 N 小时内这里没有再下过雨」

不按停留时，调 `next_city(exclude=(当前城,))`，音频不断，
换城之间插 `city_switch_gap_ms`（1200 ms）静默。

---

## 5. 可调项

| 参数 | 默认 | 说明 |
|---|---|---|
| `--rain-floor` | 0.30 mm/h | 可听下限，与抓取脚本一致 |
| `--min-hours` / `--min-peak` | 1 / 0.30 | 事件下限。默认等于不过滤，各城事件数与抓取脚本自己数的一致，整条链可对账 |
| `--wind-floor` | 3.4 m/s | 低于此值不渲染风 |
| `--thunder-min` | 0.05 | thunder_score 下限；`weather_code` 95/96/99 可越过 |
| `--calibration` | `max` | `max` = 分城分位与绝对刻度取大 |
| `--levels` | 无 | 素材响度补偿表，见 `levels.json` |
| `--slim` | 关 | 不写 `ui` 字段，体积减半 |

**标定为什么是 `max`**。绝对刻度锚在 `0.3 / 1.0 / 2.5 / 7.5 / 15 mm/h`，
15 以上一律封顶为暴雨；分城刻度锚在该城可听小时的 P50/P85/P97/max。
两者取大，于是：广州 20 mm/h 走绝对刻度到顶，拉萨 2.8 mm/h 走分城刻度也到顶。
拉萨的一场雨响度上能追平广州的暴雨——这是你定的，不是副作用。

---

## 6. 会踩的坑

**mp3 的编码器补白**。mp3 解码后头尾各有几十毫秒静音，`source.loop = true`
会在接缝处漏出一声「嗒」。试听台的做法是随机取 25 秒片段、首尾 0.4 秒
等功率交叠，既躲开补白也顺带消掉周期感——一场雨最长 90 小时，
同一条素材要循环上万次，不打散必然听出来。上线前建议把素材转成
`ogg/opus` 或 `wav`，从源头绕开这个问题。

**雷的素材只有 12 条**，远雷 2 条、干雷 10 条。引擎已经把每小时雷击频次
封顶在 12 次并禁止连续重复，但强雷小时仍会用到 20 次上下，
每条素材一小时内出现两次左右。远雷只有 2 条，实质是交替播放。
要再自然，得补素材，参数上没有更多余地。

**CIN 缺测会高估雷**。抓取脚本在 `convective_inhibition` 为空时按「无抑制」
处理，`cin_term` 强制取 1.0。北方城市这个缺测率很高——哈尔滨 58.2%、
北京 57.8%、银川 54.1%。如果上线后觉得北方雷偏多，调 `--thunder-min`，
或在 `thunder_strikes` 里对 `cin_missing == 1` 的小时打个折。

**`thunder_score` 的 λ 未标定**。抓取脚本自己注明去掉 `li_term` 后系数需要
重新标定。现在的对照是 `weather_code` 95/96/99：广州 184 小时、
新加坡 133 小时、拉萨 3 小时、银川 0 小时。用这个对账。

---

## 7. 与抓取脚本的三处口径差异

1. **降雪不发声**。上游 `audible` 含雪；这里只认液态降水，
   事件边界因此重算，`index.csv` 里的 `event_id / run_elapsed / gap_to_next /
   burstiness / character` 一律不用。雪只进 `ui`。
2. **事件重新切分**。规则与上游相同（连续可听小时，允许跨接 1 小时空档），
   只是输入的可听标记不同。被跨接的那个无雨小时增益压到 `-19 dB`，
   听起来是雨势短暂收住，不是断掉。
3. **城市数**。抓取脚本 `CITIES` 表实际是 57 条，你的方案里写的是 56 城。
   引擎不关心这个数，按 `openmeteo_out` 里实际有几份 `*_index.csv` 走。
